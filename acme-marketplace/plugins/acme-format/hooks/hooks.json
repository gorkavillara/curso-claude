{
  "description": "Bloquea git commit si el lint falla.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          { "type": "command", "command": "node ${CLAUDE_PLUGIN_ROOT}/check.js" }
        ]
      }
    ]
  }
}
